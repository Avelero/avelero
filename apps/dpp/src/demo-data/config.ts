/**
 * Demo passport configuration for the public DPP demo.
 *
 * Uses the canonical default passport so the demo and seeded brand theme stay
 * perfectly aligned.
 */

import { createDefaultPassport } from "@v1/dpp-components";

export const demoPassport = createDefaultPassport();
