/**
 * DPP Schema Version Constant
 *
 * Defines the current version of the Digital Product Passport schema.
 * This version is stored with each published passport snapshot to enable
 * future migrations and backwards compatibility.
 *
 * Update this when the snapshot structure changes significantly.
 */

export const DPP_SCHEMA_VERSION = "1.0";
