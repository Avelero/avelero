/**
 * Taxonomy query functions barrel export.
 * 
 * Taxonomy refers to global entities (like categories) that are not
 * brand-owned, as opposed to catalog entities which are brand-owned.
 */
export * from "./categories.js";





