/**
 * Unit Test Setup
 *
 * Minimal setup for unit tests - no database dependencies.
 * Unit tests run fast with pure function testing only.
 */

// No database setup needed for unit tests
// All tests in __tests__/unit/* are pure function tests without external dependencies
