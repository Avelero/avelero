import { Check, Copy, Loader2, LogOut, Settings, BarChart3, ChevronDown, Plus } from "lucide-react";

export const Icons = {
  SignOut: LogOut,
  Copy,
  Check,
  Loader: Loader2,
  Settings,
  Overview: BarChart3,
  ChevronDown,
  Plus,
};
